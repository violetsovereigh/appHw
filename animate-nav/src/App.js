import React, { Component } from 'react';
import { StackRouter } from './Router';

class App extends Component {
      render(){
            return(
                <StackRouter />  
            );
      }
}

export default App;
